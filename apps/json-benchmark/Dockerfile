# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build json-benchmark`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t json-benchmark`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system json-benchmark && \
          adduser --system -G json-benchmark json-benchmark

COPY dist/apps/json-benchmark json-benchmark/
RUN chown -R json-benchmark:json-benchmark .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix json-benchmark --omit=dev -f install

CMD [ "node", "json-benchmark" ]
